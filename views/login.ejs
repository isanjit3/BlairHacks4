<!DOCTYPE html>
<html lang="en">

<head>

    <%- include ("partials/head") %>

    <style>
        .container {
            margin-top: 100px;
        }
    </style>
</head>

<body>
    <div class="container  animated fadeIn">
        <div class="card mx-auto" style="width: 400px">
            <div class="card-header">
                <h5>Login</h5>
            </div>
            <div class="card-body">
                <div class="form-group">
                    <label>Username</label>
                    <input class="form-control" type="text" id="username" size="6">
                </div>
                <div class="form-group">
                    <label>Password</label>
                    <input class="form-control" type="password" id="password" size="6">
                </div>
                <div class="form-group text-center">
                    <button class="btn btn-md btn-success" type="button" id="loginBtn"><a
                            href="/index">Login</a></button>
                </div>
            </div>
        </div>
    </div>
    <!--
    <script>
        
            $(document).ready(function () {
            $('#loginBtn').on('click', function () {
                if (!$('#username').val()) {
                    alert('Please enter the username')
                    return
                }
                if (!$('#password').val()) {
                    alert('Please enter the password')
                    return
                }
                $.ajax({
                    type: "POST",
                    url: '/login',
                    data: {
                        username: $('#username').val(), 
                        password: $('#password').val()
                    },
                    success: function(data) {
                        if(data.auth == true) {
                            window.sessionStorage.token = data.token
                            window.location.replace('/index')
                        } else {
                            alert('Log in failed')
                        }
                    },
                    error: function(xhr, status, error, responseText) {
                        if(error == "Unauthorized") {
                            alert('Invalid username or password')
                        } else {
                            alert('Failed to validate user')
                        }
                    },
                    dataType: 'json'
                });
            })
        })
            
        
    </script>
    -->
</body>

</html>